require 'spec_helper'

describe LjBaiduPush do
  it 'has a version number' do
    expect(LjBaiduPush::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
